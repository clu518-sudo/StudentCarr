# StudentCarr Demo App

Static Vite + React frontend for the StudentCarr visitor demo.

```powershell
npm ci
npm run dev
```

Use the **Continue as Visitor** button. All displayed data is local demo data and all backend-dependent actions are disabled.
